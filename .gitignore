# Python
__pycache__/
*.py[cod]
*.so
.venv/
venv/
env/
.env
.envrc

# Runtime state (generated/modified at runtime, contain local paths)
settings.json
simulators.json

# GPG keyring created at runtime by the maintainer key management feature
.gnupg/

# Background build job state (ephemeral, may contain large log output)
.jobs/

# Uploaded XML files (transient)
uploads/

# Editor
.vscode/
.idea/
